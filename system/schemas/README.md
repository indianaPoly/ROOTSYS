# schemas

Entry point for machine-readable schema contracts.

## Purpose
- Scope: JSON Schema artifacts used to validate external interface definitions.
- Owner: repository maintainers (`@indianaPoly`).

## Contents
- `system/schemas/external_interface.schema.json`: JSON Schema for `ExternalInterface`.
