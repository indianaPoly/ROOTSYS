[package]
name = "shell"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
common = { path = "../common" }
drivers = { path = "../drivers" }
runtime = { path = "../runtime" }
serde_json = "1.0"
