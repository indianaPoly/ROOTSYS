# crates (한국어)

ROOTSYS 런타임 실행을 구성하는 워크스페이스 크레이트 모음입니다.

## 포함 크레이트
- `common`: 공통 데이터 모델
- `drivers`: 입력 어댑터(file/rest/db/stream mvp)
- `runtime`: 검증/통합 파이프라인 코어
- `shell`: CLI 실행 진입점
- `fabric`: 통합 결과 병합 레이어
- `ontology`, `linkage`, `kernel`: product-flow 모듈
